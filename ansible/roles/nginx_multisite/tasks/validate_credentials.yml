---
- name: sub | Validate SSL certificate credentials
  ansible.builtin.assert:
    that:
      - ssl_cert_content is defined
      - ssl_key_content is defined
    fail_msg: SSL certificate and key credentials must be provided via AAP vault or credential type.
    success_msg: SSL certificate credentials are available.
  tags:
    - always
